/**
 * 모든 사진의 가로세로비 — **`4:5` 하나로 고정**. 인스타그램의 세로 최대치와 같다.
 *
 * **보여줄 때만 쓴다.** 찍힌 것·고른 것은 제 비율 그대로 남고,
 * 컴포저 · 피드 · 달력 · 저장 카드가 `cover` 로 가운데를 보여줄 뿐이다.
 *
 * 하나로 고정해야 칸에서 상세로 날아가는 동안 모양이 안 변하고,
 * 원본 비율을 안 재므로 사진이 늦게 로드돼도 칸 높이가 안 흔들린다.
 */
export const PHOTO_RATIO = 4 / 5

/**
 * 개발 중에는 **저장된 세션을 복원하지 않는다.**
 *
 * 토큰 유효성은 확인하지 않고 있는지만 보므로, 한 번 로그인하면 그 뒤로는 늘 홈으로 곧장 들어가
 * 첫 진입 화면을 다시 볼 수 없다. 도메인이 전부 목업이라 401 로 튕겨나는 일도 없다.
 *
 * 대신 로그인 화면 우상단의 `바로 들어가기` 로 서버 없이 한 번에 통과한다.
 * 늘 홈부터 시작하고 싶으면 이 값을 `false` 로 둔다.
 */
export const START_AT_LOGIN = __DEV__

/**
 * 서버 없이 인메모리 목업으로 동작 — **인증과 도메인이 같은 스위치를 쓴다.**
 *
 * 한때 도메인만 목업이고 인증은 실서버였다. 그러면 서버를 띄우지 않는 한
 * 로그인·가입 화면을 지나갈 수 없어서 그 뒤 화면들을 아예 못 본다.
 * 백엔드가 준비되면 이 값 하나만 `false` 로 바꾼다.
 */
export const USE_MOCK = true
