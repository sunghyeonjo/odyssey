/**
 * Dayed 팔레트.
 *
 * **아주 옅은 회색 바탕 위에 순백 카드.** 층은 선이 아니라 면으로 가른다 —
 * 바탕을 한 단만 내려두면 카드가 저절로 떠 보여서 테두리를 두를 일이 없다.
 *
 * 무채색만 쓴다. 포인트도 색이 아니라 **잉크**이고, 화면의 진짜 색은 **사진에서만** 나온다.
 * (크림·브라운 톤을 걷어내고 여기로 왔다)
 */
export const colors = {
  /**
   * 주 동작 · 오늘 표시 · 배지.
   *
   * 순검정이 아니라 **깊은 슬레이트**다. 흰 바탕에서 순검정 면은 화면에 구멍을 뚫은 것처럼
   * 강해서, 하루에 한 번 누르는 버튼이 늘 화면의 주인이 되어버린다.
   * 살짝 푸른 기가 도는 짙은 색은 같은 위계를 지키면서 훨씬 조용하다.
   */
  accent: '#3B4657',
  /** 입력칸 · 보조 면 · 글만 있는 기록의 종이 */
  accentSoft: '#EDF0F4',
  /** 달력 칸 바탕 — 순백 카드 위에 한 단 내려앉은 면 */
  tile: '#F1F4F7',
  /**
   * 글자는 버튼보다 진해도 된다 — 면이 아니라 획이라 같은 검기여도 무게가 다르다.
   * 중성색 전부에 아주 옅은 한기를 넣었다. 완전한 무채색 회색은 흰 옆에서 탁해 보인다
   */
  ink: '#191D24',
  sub: '#697280',
  faint: '#A6AEBA',
  line: '#E5EAF0',
  /** 바탕 — 순백 카드가 떠 보이게 한 단 내린다 */
  bg: '#F4F6F9',
  surface: '#FFFFFF',
  /**
   * 화면을 통째로 덮는 어두운 무대 — 지금은 사진 자르기 하나뿐이다.
   * 사진이 유일한 주인공이 되는 자리라 UI 를 검은 쪽으로 완전히 물린다.
   * 순검정이 아니라 아주 살짝 푸른 것도 같은 이유 — 중성색 전부가 그렇다
   */
  stage: '#0B0D11',
  danger: '#E5484D',
  ok: '#2F9E68',
  /**
   * 달력의 주말 — **무채색 규칙의 유일한 예외**다.
   * 달력에서 토·일을 못 가리면 한 주가 어디서 끊기는지 매번 세어야 한다.
   * 경고(`danger`)의 빨강보다 한 단 죽여서, 주말이 사고처럼 보이지 않게 한다
   */
  sun: '#C25A54',
  sat: '#5A76A0',
} as const

export const radius = { chip: 8, photo: 12, tile: 14, card: 22, sheet: 22, pill: 999 } as const

/**
 * 글자 크기 — 시스템 서체(SF Pro / Roboto) 기준 일곱 단.
 * 굵기는 400(본문) · 600(강조) · 700(제목) 셋만 쓴다.
 * 화면마다 눈대중으로 맞추면 0.5 단위로 갈라진다. 여기 없는 크기는 쓰지 않는다.
 */
export const text = {
  /** 우표 안 날짜, 카드 반응 수 */
  micro: 11,
  /** 메타 · 핸들 · 라벨 */
  caption: 12,
  /** 보조 문구 */
  small: 13,
  /** 본문 · 목록 */
  body: 14,
  /** 행 제목 · 버튼 · 입력 */
  lead: 15,
  /** 화면 제목 · 날짜 머리글 */
  title: 18,
  /** 큰 숫자 · 구역 머리글 — 가장 큰 단. 표제로 40 을 쓰던 자리는 얇은 막대로 바뀌었다 */
  display: 22,
} as const

/** 간격 — 4의 배수 다섯 단. 5·7·9 처럼 사이값을 쓰면 리듬이 깨진다 */
export const space = { xs: 4, sm: 8, md: 12, lg: 16, xl: 24 } as const
